target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar
.project
.classpath
.settings
.idea/
.metadata
*.iml
bin/

# Git Bash on Windows artefact: `> nul` is not a device here, so it creates a FILE named `nul`.
# And `nul` is a reserved Windows name, so committing it breaks every clone on Windows.
nul
